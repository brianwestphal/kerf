node_modules/
dist/
!tests/dist/
# KF-123: consumer-app emits its bundled output here at test time. Keep the
# source committed (src/, index.html, build.mjs, tsconfig.json) but ignore
# the rebuilt artifact.
tests/dist/consumer-app/dist/
# KF-165: example-apps' per-app Vite output is rebuilt at globalSetup time;
# keep `tests/dist/example-apps/build.mjs` committed but ignore the bundles.
tests/dist/example-apps/*/
coverage/
playwright-report/
test-results/
*.log
*.tsbuildinfo
.DS_Store
.claude/
.glassbox/
.hotsheet/
.mcp.json

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# Release-script state
.release-state.json

# npm pack artifacts
*.tgz

# Local benchmark cache (cloned upstream + generated artifacts)
bench/.bench-cache/
