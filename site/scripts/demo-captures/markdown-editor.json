{
  "width": 1000,
  "height": 560,
  "output": "site/public/demos/markdown-editor.svg",
  "optimize": true,
  "colorScheme": "dark",
  "vars": { "base": "http://localhost:4188" },
  "frames": [
    {
      "input": "${base}/markdown-editor/",
      "waitFor": ".editor-input",
      "wait": 350,
      "duration": 1200
    },
    {
      "continue": true,
      "actions": [
        { "type": "evaluate", "script": "(()=>{const ed=document.querySelector('.editor-input');ed.focus();ed.innerText=ed.innerText.replace('# Type fast. The cursor stays put.','# Edited live — caret never jumps\\n\\nType in the **middle** of a line and keep going.');ed.dispatchEvent(new InputEvent('input',{bubbles:true}));})()" },
        { "type": "wait", "ms": 250 }
      ],
      "transition": { "type": "crossfade", "duration": 260 },
      "duration": 1600
    }
  ]
}
