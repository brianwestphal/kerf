{
  "$schema": "https://json.schemastore.org/sarif-2.1.0.json",
  "version": "2.1.0",
  "runs": [
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mrxyaiily8qr9w6p",
          "message": {
            "text": "The two KF-381 shapes are pinned as .skip tests asserting the INTENDED contract, not as passing tests asserting the buggy duplicate-rows output. Rationale: asserting duplication as expected would make the KF-381 fix a red-to-green flip in the wrong direction and would put a wrong contract in the suite; .skip keeps the correct contract readable, and un-skipping these two tests is the fix's acceptance criterion. The npm run check gate stays green because skipped tests don't run.",
            "markdown": "The two KF-381 shapes are pinned as .skip tests asserting the INTENDED contract, not as passing tests asserting the buggy duplicate-rows output. Rationale: asserting duplication as expected would make the KF-381 fix a red-to-green flip in the wrong direction and would put a wrong contract in the suite; .skip keeps the correct contract readable, and un-skipping these two tests is the fix's acceptance criterion. The npm run check gate stays green because skipped tests don't run."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "tests/unit/kf380-interaction-matrix.test.tsx"
                },
                "region": {
                  "startLine": 270,
                  "endLine": 270,
                  "snippet": {
                    "text": "  it.skip('a conditional sibling INSIDE the list parent before the marker keeps rows single (KNOWN BUG KF-381 shape 1)', () => {"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-381"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "c146673e1aeb127c0ee5825a7880a999"
          }
        },
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mrxyaioflyr8tp36",
          "message": {
            "text": "The render-count pin is 4, not 3: arraySignal mutations re-run the mount render by design (that render is how the patch queue drains), so initial + two banner toggles + the granular remove = 4. The invariant this test asserts is that the four SELECTION flips add zero renders — they flow entirely through the per-row binding effects. If this pin breaks with a higher count, a selection flip started re-rendering (the FC-B3 regression); if lower, the patch-drain model changed.",
            "markdown": "The render-count pin is 4, not 3: arraySignal mutations re-run the mount render by design (that render is how the patch queue drains), so initial + two banner toggles + the granular remove = 4. The invariant this test asserts is that the four SELECTION flips add zero renders — they flow entirely through the per-row binding effects. If this pin breaks with a higher count, a selection flip started re-rendering (the FC-B3 regression); if lower, the patch-drain model changed."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "tests/unit/kf380-interaction-matrix.test.tsx"
                },
                "region": {
                  "startLine": 372,
                  "endLine": 374,
                  "snippet": {
                    "text": "    // Selection flips never re-ran the render: only the initial render, the\n    // two banner toggles, and the granular remove (patch drain) did.\n    expect(render).toHaveBeenCalledTimes(4);"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-380"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "d95518d0b19fa6629c8937092d693450"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/kerf.git",
          "revisionId": "ba66de0807f201b113f1998fa6e0da7ee6cce723",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mry26rhpb2zpb471",
          "message": {
            "text": "This test exists because verifying the WONTFIX escape hatch found the documented guidance was half-wrong. Measured, not reasoned: keying the CONDITIONAL SIBLING preserves row identity on toggle-OFF but NOT toggle-ON (the template's keyed banner has no live counterpart, the keyed lookup misses, and the positional fallback tests only the LIVE node's key, so the unkeyed container is hijacked anyway). Keying the LIST'S OWN container works in both directions and across repeats, because the key makes it simultaneously ineligible for positional matching and findable by key. docs/4 §4.2, docs/11 §11.2.9, the KERF_DEV_WARN_LIST_REBIND message, and the CHANGELOG were all corrected from the sibling-keying advice to container-keying; this test guards the corrected guidance so it cannot rot.",
            "markdown": "This test exists because verifying the WONTFIX escape hatch found the documented guidance was half-wrong. Measured, not reasoned: keying the CONDITIONAL SIBLING preserves row identity on toggle-OFF but NOT toggle-ON (the template's keyed banner has no live counterpart, the keyed lookup misses, and the positional fallback tests only the LIVE node's key, so the unkeyed container is hijacked anyway). Keying the LIST'S OWN container works in both directions and across repeats, because the key makes it simultaneously ineligible for positional matching and findable by key. docs/4 §4.2, docs/11 §11.2.9, the KERF_DEV_WARN_LIST_REBIND message, and the CHANGELOG were all corrected from the sibling-keying advice to container-keying; this test guards the corrected guidance so it cannot rot."
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "tests/unit/kf380-interaction-matrix.test.tsx"
                },
                "region": {
                  "startLine": 478,
                  "endLine": 515,
                  "snippet": {
                    "text": "  it('KF-383: keying the LIST CONTAINER is the documented fix for the shape above — identity survives both directions', () => {\n    // The escape hatch kerf documents for the shape-2 trade-off, guarded here\n    // so the guidance can't rot. A key on the list's own container makes it\n    // ineligible for positional matching AND findable by key, so neither\n    // toggle direction can hijack it.\n    //\n    // NOTE the asymmetry, verified empirically: keying the CONDITIONAL SIBLING\n    // instead only fixes the toggle-OFF direction. On toggle-ON the template's\n    // keyed banner has no live counterpart, the keyed lookup misses, and the\n    // fallback positional match tests only the LIVE node's key — so the\n    // unkeyed live container is hijacked anyway. Key the container, not the\n    // sibling. (Wrapping the sibling in an always-present container also\n    // works, but costs an element.)\n    const banner = signal(true);\n    const dispose = mount(root, () => (\n      <div>\n        {banner.value ? <ul class=\"banner\"><li class=\"hd\">warn</li></ul> : ''}\n        <ul class=\"list\" data-key=\"the-list\">\n          {each(ROWS, (r) => <li data-key={r.id}>{r.label}</li>)}\n        </ul>\n      </div>\n    ));\n    expect(labels()).toEqual(['A', 'B']);\n    const rowA = root.querySelector('li[data-key=\"a\"]');\n\n    banner.value = false;\n    expect(labels()).toEqual(['A', 'B']);\n    expect(root.querySelector('li[data-key=\"a\"]')).toBe(rowA);\n\n    banner.value = true;\n    expect(labels()).toEqual(['A', 'B']);\n    expect(root.querySelector('li[data-key=\"a\"]')).toBe(rowA);\n    expect(labels(root.querySelector('ul.banner') as HTMLElement)).toEqual([]);\n\n    banner.value = false; // and it holds across repeats\n    expect(root.querySelector('li[data-key=\"a\"]')).toBe(rowA);\n    dispose();\n  });"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "test-evidence"
            ]
          },
          "workItemUris": [
            "KF-383"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "300a88783ece298d5c7584e079741e4f"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/kerf.git",
          "revisionId": "1a288d94a75f45f4592796fb3e8b8ffe04d72d41",
          "branch": "main"
        }
      ]
    }
  ]
}
