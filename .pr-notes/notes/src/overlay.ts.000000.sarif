{
  "$schema": "https://json.schemastore.org/sarif-2.1.0.json",
  "version": "2.1.0",
  "runs": [
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mszggv7xtlf43q6s",
          "message": {
            "text": "The result mapping keys on typeof value === 'string', not truthiness — so an empty string is a valid OK result, distinct from null dismissal.\n\nprompt() resolves three distinct DOM outcomes onto two logical ones: OK → close(value) (a string, possibly ''), Cancel → close(null), and Escape/backdrop dismissal → the overlay's default close(undefined). Mapping typeof value === 'string' ? value : null collapses both cancel and dismissal to null while preserving '' as a genuine result. A truthiness check (value || null) would be wrong here: it would turn a user who cleared the field and pressed OK into a null, indistinguishable from cancel. The unit test \"an empty string is a valid OK result (not null)\" pins exactly this. form() uses the analogous value !== null && typeof value === 'object' guard for the same reason — a {} record from an all-empty form is still a submit, not a cancel.\n",
            "markdown": "The result mapping keys on `typeof value === 'string'`, not truthiness — so an empty string is a valid OK result, distinct from `null` dismissal.\n\n`prompt()` resolves three distinct DOM outcomes onto two logical ones: OK → `close(value)` (a string, possibly `''`), Cancel → `close(null)`, and Escape/backdrop dismissal → the overlay's default `close(undefined)`. Mapping `typeof value === 'string' ? value : null` collapses both cancel and dismissal to `null` while preserving `''` as a genuine result. A truthiness check (`value || null`) would be wrong here: it would turn a user who cleared the field and pressed OK into a `null`, indistinguishable from cancel. The unit test \"an empty string is a valid OK result (not null)\" pins exactly this. `form()` uses the analogous `value !== null && typeof value === 'object'` guard for the same reason — a `{}` record from an all-empty form is still a submit, not a cancel.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/overlay.ts"
                },
                "region": {
                  "startLine": 394,
                  "endLine": 394,
                  "snippet": {
                    "text": "  return handle.result.then((value) => (typeof value === 'string' ? value : null));"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-484"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "51c97aca10f98234cbaa11de6e2607dc"
          }
        },
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mszgmyto0liwdjhb",
          "message": {
            "text": "Non-null assertions here are correctness-honest, not laziness — and they keep coverage truthful.\n\nprompt()/form() render [data-prompt-input], [data-prompt-error], [data-field], and [data-field-error] unconditionally into their OWN handle.el wrapper before these queries run, so the lookups cannot return null. The first draft guarded each with if (el !== null) / el?.x ?? ''. Those guards were dead by construction — every one added an else/nullish branch the tests could never reach, which dropped global branch coverage below the 98.5% gate. Asserting non-null (querySelector(...)!, find(...)!) states the real invariant and removes the phantom branches, so the coverage number reflects genuinely-unreachable code rather than being padded by guards for a null that can't occur. no-non-null-assertion is off in eslint.config.js, so this is a sanctioned, deliberate use — not a lint escape.\n",
            "markdown": "Non-null assertions here are correctness-honest, not laziness — and they keep coverage truthful.\n\n`prompt()`/`form()` render `[data-prompt-input]`, `[data-prompt-error]`, `[data-field]`, and `[data-field-error]` unconditionally into their OWN `handle.el` wrapper before these queries run, so the lookups cannot return null. The first draft guarded each with `if (el !== null)` / `el?.x ?? ''`. Those guards were dead by construction — every one added an else/nullish branch the tests could never reach, which dropped global branch coverage below the 98.5% gate. Asserting non-null (`querySelector(...)!`, `find(...)!`) states the real invariant and removes the phantom branches, so the coverage number reflects genuinely-unreachable code rather than being padded by guards for a null that can't occur. `no-non-null-assertion` is off in `eslint.config.js`, so this is a sanctioned, deliberate use — not a lint escape.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/overlay.ts"
                },
                "region": {
                  "startLine": 362,
                  "endLine": 365,
                  "snippet": {
                    "text": "\n  // Both elements are rendered unconditionally into this call's own wrapper, so\n  // the queries cannot miss (asserted non-null rather than guarded).\n  const input = handle.el.querySelector<HTMLInputElement>('[data-prompt-input]')!;"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-484"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "0fc0b6a921086df621a3655c64ed7536"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/kerf.git",
          "revisionId": "dbf262bcf4a8a9eb600b241f90e0489a7077fbd1",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mszl3fh5qz7gy07y",
          "message": {
            "text": "The reposition listeners are torn down via handle.result.then(...), not by reaching into overlay()'s private close path — so popover composes over overlay without a new coupling.\n\noverlay() resolves its result promise on EVERY close path (Escape, backdrop/outside click, or a programmatic close()), so awaiting it is a public, path-agnostic \"when this overlay goes away\" hook. Using it to remove the scroll/resize listeners means popover needs no privileged access to overlay's internal removers array and can't fall out of sync with a future dismissal trigger. The scroll listener is registered with capture: true because scrolling happens on inner scroll containers, not just window — a bubbling scroll listener would miss those and the popover would drift off its anchor. The unit test \"removes the reposition listeners on close\" asserts both removeEventListener calls fire (with the matching capture flag) after close.\n",
            "markdown": "The reposition listeners are torn down via `handle.result.then(...)`, not by reaching into `overlay()`'s private close path — so `popover` composes over `overlay` without a new coupling.\n\n`overlay()` resolves its `result` promise on EVERY close path (Escape, backdrop/outside click, or a programmatic `close()`), so awaiting it is a public, path-agnostic \"when this overlay goes away\" hook. Using it to remove the `scroll`/`resize` listeners means `popover` needs no privileged access to `overlay`'s internal `removers` array and can't fall out of sync with a future dismissal trigger. The `scroll` listener is registered with `capture: true` because scrolling happens on inner scroll containers, not just `window` — a bubbling `scroll` listener would miss those and the popover would drift off its anchor. The unit test \"removes the reposition listeners on close\" asserts both `removeEventListener` calls fire (with the matching `capture` flag) after close.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/overlay.ts"
                },
                "region": {
                  "startLine": 634,
                  "endLine": 640,
                  "snippet": {
                    "text": "  reposition();\n  window.addEventListener('scroll', reposition, true); // capture: catch scrolls in any container\n  window.addEventListener('resize', reposition);\n  void handle.result.then(() => {\n    window.removeEventListener('scroll', reposition, true);\n    window.removeEventListener('resize', reposition);\n  });"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-485"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "bd5117d4afbad7df62e50d9a197093cc"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/kerf.git",
          "revisionId": "871e2229d729af9c402bdf9b78f524651a673aaf",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mszs55vr9hfb0hlg",
          "message": {
            "text": "positionAnchored is the single source of truth for anchored placement — popover, autoReposition, and tooltip all route through it, so there's no second copy of the flip/clamp math to drift.\n\nKF-491 asked to export popover's positioning \"guts, minus mount/dismiss.\" Rather than duplicate the below/flip-above/horizontal-clamp logic into a standalone util, the reconcile inverted the dependency: positionAnchored (one-shot) holds the math, autoReposition wraps it with the scroll(capture)/resize listeners + a disposer, and popover now just calls autoReposition instead of carrying its own inline reposition(). tooltip composes the same autoReposition over an overlay(dismiss:false). The pre-existing popover unit tests (flip-above, horizontal-clamp, align:end, reposition-on-scroll, listener-cleanup) passed unchanged through the refactor, which is the evidence the extraction is behavior-preserving — the placement contract is identical, just relocated.\n",
            "markdown": "`positionAnchored` is the single source of truth for anchored placement — `popover`, `autoReposition`, and `tooltip` all route through it, so there's no second copy of the flip/clamp math to drift.\n\nKF-491 asked to export popover's positioning \"guts, minus mount/dismiss.\" Rather than duplicate the below/flip-above/horizontal-clamp logic into a standalone util, the reconcile inverted the dependency: `positionAnchored` (one-shot) holds the math, `autoReposition` wraps it with the scroll(capture)/resize listeners + a disposer, and `popover` now just calls `autoReposition` instead of carrying its own inline `reposition()`. `tooltip` composes the same `autoReposition` over an `overlay(dismiss:false)`. The pre-existing popover unit tests (flip-above, horizontal-clamp, align:end, reposition-on-scroll, listener-cleanup) passed unchanged through the refactor, which is the evidence the extraction is behavior-preserving — the placement contract is identical, just relocated.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/overlay.ts"
                },
                "region": {
                  "startLine": 570,
                  "endLine": 570,
                  "snippet": {
                    "text": "  // Vertical: preferred side, flipped only if it overflows and the other side fits."
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-491"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "1d30f2357e8734b72075d382f6bff6ea"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/kerf.git",
          "revisionId": "01580499cc62f99d2f58a136c4198dc152fcf829",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mszscb0mn38yn8zj",
          "message": {
            "text": "mode:'replace' needs to dismiss the region's OTHER toasts, but that active set is stored ON the region element (a Symbol prop), not in a module-level variable — so toast() keeps zero module state, consistent with toastRegion() which already keeps \"the region\" in the DOM rather than a module singleton.\n\nA mode:'replace' toast can't just region.innerHTML = '' to clear the others: that would strip their DOM without running each one's mount() disposer (leaking the mount) and without honoring an in-flight exitClass transition. It needs their real dismiss fns. The obvious place to keep them is a module-level WeakMap<region, Set<dismiss>>, but that reintroduces module-level mutable state this file was designed to avoid (Design rule 5). Hanging the Set off the region element via region[TOAST_SET] puts the state where its lifetime already lives — GC'd with the region, scoped per region for free (a custom container gets its own set), and invisible to grep '^let'. Iterating a COPY ([...active]) before dismissing matters: each dismiss calls active.delete(itself), which would corrupt a live iteration.\n",
            "markdown": "`mode:'replace'` needs to dismiss the region's OTHER toasts, but that active set is stored ON the region element (a Symbol prop), not in a module-level variable — so toast() keeps zero module state, consistent with `toastRegion()` which already keeps \"the region\" in the DOM rather than a module singleton.\n\nA `mode:'replace'` toast can't just `region.innerHTML = ''` to clear the others: that would strip their DOM without running each one's `mount()` disposer (leaking the mount) and without honoring an in-flight `exitClass` transition. It needs their real `dismiss` fns. The obvious place to keep them is a module-level `WeakMap<region, Set<dismiss>>`, but that reintroduces module-level mutable state this file was designed to avoid (Design rule 5). Hanging the `Set` off the region element via `region[TOAST_SET]` puts the state where its lifetime already lives — GC'd with the region, scoped per region for free (a custom `container` gets its own set), and invisible to `grep '^let'`. Iterating a COPY (`[...active]`) before dismissing matters: each `dismiss` calls `active.delete(itself)`, which would corrupt a live iteration.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/overlay.ts"
                },
                "region": {
                  "startLine": 838,
                  "endLine": 842,
                  "snippet": {
                    "text": "\n  const region = toastRegion(container) as ToastCarrier;\n  const active = (region[TOAST_SET] ??= new Set<() => void>());\n  if (mode === 'replace') for (const d of [...active]) d(); // collapse-to-latest\n"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-487"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "59aa32e4999d21277d3ee4f89587a28f"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/kerf.git",
          "revisionId": "d4bfb65b9a559f3643278d6461557b42ecbca5ca",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mszsnm5xs9u27k44",
          "message": {
            "text": "The BYO render API is small because the dialogs were already wired by DATA ATTRIBUTE, not by class — render only swaps the body; nothing in the resolve/validate path changed.\n\nconfirm/prompt/form delegate on [data-confirm] / [data-prompt] / [data-form] and look up fields via [data-prompt-input] / [data-field] / [data-field-error]. So supporting caller-supplied markup needed exactly three things, none of them touching the promise/validate/Enter logic: (1) branch body to the caller's render(slots) when provided; (2) hand the caller the wiring as spreadable attribute bags (ok: { 'data-confirm': 'ok' }, etc.) so their elements carry the same hooks; (3) move initialFocus from the kerf class selectors (.kerf-confirm__ok) to the data-attribute selectors ([data-confirm=\"ok\"]) so focus lands in BYO markup too. The one behavioral softening: the error slot became optional (nullable-guarded in prompt/form) because a design-system dialog without validate shouldn't be forced to include an error element — covered by the \"render without an error slot\" tests, which exercise the null arms the default-markup tests can't reach.\n",
            "markdown": "The BYO `render` API is small because the dialogs were already wired by DATA ATTRIBUTE, not by class — `render` only swaps the body; nothing in the resolve/validate path changed.\n\n`confirm`/`prompt`/`form` delegate on `[data-confirm]` / `[data-prompt]` / `[data-form]` and look up fields via `[data-prompt-input]` / `[data-field]` / `[data-field-error]`. So supporting caller-supplied markup needed exactly three things, none of them touching the promise/validate/Enter logic: (1) branch `body` to the caller's `render(slots)` when provided; (2) hand the caller the wiring as spreadable attribute bags (`ok: { 'data-confirm': 'ok' }`, etc.) so their elements carry the same hooks; (3) move `initialFocus` from the kerf class selectors (`.kerf-confirm__ok`) to the data-attribute selectors (`[data-confirm=\"ok\"]`) so focus lands in BYO markup too. The one behavioral softening: the error slot became optional (nullable-guarded in prompt/form) because a design-system dialog without `validate` shouldn't be forced to include an error element — covered by the \"render without an error slot\" tests, which exercise the null arms the default-markup tests can't reach.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/overlay.ts"
                },
                "region": {
                  "startLine": 265,
                  "endLine": 267,
                  "snippet": {
                    "text": "  const body: OverlayContent = render !== undefined\n    ? render({ message, ok: { 'data-confirm': 'ok' }, cancel: { 'data-confirm': 'cancel' } })\n    : jsx('div', {"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-490"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "0265eaa23c235c89e7ffeb95b301a036"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/kerf.git",
          "revisionId": "e2f25d1ce8e9454aa841b0b2cba282f56008160a",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mszzdlu3qmxnxxrv",
          "message": {
            "text": "finish(instant) unifies dismissal so mode:'replace' can drop a prior toast WITHOUT its exit transition — the fix for a centered single-slot region.\n\nThe old dismiss() always ran the exit (add exitClass, wait exitDuration, remove). That's right for a stacking region, but for a single position:fixed; left:50% slot the outgoing message fades out at the exact coordinates the incoming one fades in, so for exitDuration ms two different messages cross-fade through each other. collapse:'instant' needs a synchronous teardown path, so each toast now exposes .removeNow() (finish(true) → immediate remove()) alongside the public dismiss() (finish(false) → transition), and mode:'replace' picks between them. Two smaller corrections live in the same finish(false) branch: the entrance enterClass is REMOVED before exitClass is added (so exitClass no longer has to out-specify a still-present enter class — the KF-495 footgun), and removal is delayed whenever exitClass is set OR exitDuration > 0, which makes the symmetric single-class idiom (enter = add .visible, exit = remove it, no separate exit class) actually fade out instead of vanishing instantly.\n",
            "markdown": "`finish(instant)` unifies dismissal so `mode:'replace'` can drop a prior toast WITHOUT its exit transition — the fix for a centered single-slot region.\n\nThe old `dismiss()` always ran the exit (add `exitClass`, wait `exitDuration`, remove). That's right for a stacking region, but for a single `position:fixed; left:50%` slot the outgoing message fades out at the exact coordinates the incoming one fades in, so for `exitDuration` ms two different messages cross-fade through each other. `collapse:'instant'` needs a synchronous teardown path, so each toast now exposes `.removeNow()` (`finish(true)` → immediate `remove()`) alongside the public `dismiss()` (`finish(false)` → transition), and `mode:'replace'` picks between them. Two smaller corrections live in the same `finish(false)` branch: the entrance `enterClass` is REMOVED before `exitClass` is added (so `exitClass` no longer has to out-specify a still-present enter class — the KF-495 footgun), and removal is delayed whenever `exitClass` is set OR `exitDuration > 0`, which makes the symmetric single-class idiom (enter = add `.visible`, exit = remove it, no separate exit class) actually fade out instead of vanishing instantly.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/overlay.ts"
                },
                "region": {
                  "startLine": 988,
                  "endLine": 1004,
                  "snippet": {
                    "text": "    if (state.dismissed) return;\n    state.dismissed = true;\n    if (state.timer !== undefined) clearTimeout(state.timer);\n    if (instant) {\n      remove();\n      return;\n    }\n    // Reverse the entrance (drop enterClass so exitClass needn't out-specify it),\n    // add exitClass, and delay removal for the transition — including the\n    // single-class idiom (enterClass removed + exitDuration, no exitClass).\n    if (enterClass !== undefined) el.classList.remove(enterClass);\n    if (exitClass !== undefined) el.classList.add(exitClass);\n    if (exitClass !== undefined || exitDuration > 0) {\n      setTimeout(remove, exitDuration);\n    } else {\n      remove();\n    }"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-495"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "82401b73336b11e13612fb830b2bf905"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/kerf.git",
          "revisionId": "0ccc726a2c13a67e52839d1c8db976370173251d",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mszzqm4k6u88j05s",
          "message": {
            "text": "choice() resolves its OWN promise instead of mapping overlay's result value — so an action whose value is null or undefined stays distinct from a dismissal.\n\nconfirm/prompt/form map the value passed to handle.close() (dismissal arrives as undefined), which works because their result domains never include undefined. choice<R> is generic — an action's value can legitimately BE null or undefined (a \"none\"/\"skip\" outcome), so overlay's dismissal-undefined and a real undefined value would collide. The fix is a private promise: a click/Enter calls resolveChoice(actions[i].value) and handle.result.then(() => resolveChoice(null)) supplies the dismissal fallback. Promise resolution is first-wins, so a click's value stands and the later fallback null is ignored; a dismissal reaches only the fallback. This also gives the \"global Enter-to-confirm\" model (defaultValue resolved on Enter anywhere in the dialog) without exposing the overlay handle — the specific thing KF-494 said confirm couldn't do. 'defaultValue' in options (not !== undefined) gates the Enter handler so defaultValue: undefined still opts in.\n",
            "markdown": "`choice()` resolves its OWN promise instead of mapping `overlay`'s result value — so an action whose `value` is `null` or `undefined` stays distinct from a dismissal.\n\n`confirm`/`prompt`/`form` map the value passed to `handle.close()` (dismissal arrives as `undefined`), which works because their result domains never include `undefined`. `choice<R>` is generic — an action's `value` can legitimately BE `null` or `undefined` (a \"none\"/\"skip\" outcome), so overlay's dismissal-`undefined` and a real `undefined` value would collide. The fix is a private promise: a click/Enter calls `resolveChoice(actions[i].value)` and `handle.result.then(() => resolveChoice(null))` supplies the dismissal fallback. Promise resolution is first-wins, so a click's value stands and the later fallback `null` is ignored; a dismissal reaches only the fallback. This also gives the \"global Enter-to-confirm\" model (`defaultValue` resolved on Enter anywhere in the dialog) without exposing the overlay handle — the specific thing KF-494 said `confirm` couldn't do. `'defaultValue' in options` (not `!== undefined`) gates the Enter handler so `defaultValue: undefined` still opts in.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/overlay.ts"
                },
                "region": {
                  "startLine": 710,
                  "endLine": 735,
                  "snippet": {
                    "text": "  // Own the promise (not overlay's result value) so an action `value` of\n  // `undefined`/`null` stays distinct from a dismissal.\n  let resolveChoice!: (r: R | null) => void;\n  const result = new Promise<R | null>((resolve) => {\n    resolveChoice = resolve;\n  });\n\n  const handle = overlay(body, {\n    container,\n    className,\n    dismiss: ['escape', 'backdrop'],\n    initialFocus: '[data-choice]',\n    trap: true,\n  });\n\n  delegate(handle.el, 'click', '[data-choice]', (_event, el) => {\n    resolveChoice(actions[Number(el.getAttribute('data-choice'))].value);\n    handle.close();\n  });\n\n  if (hasDefault) {\n    handle.el.addEventListener('keydown', (event: KeyboardEvent) => {\n      if (event.key === 'Enter') {\n        event.preventDefault();\n        resolveChoice(defaultValue as R);\n        handle.close();"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "KF-494"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "e26587ebbd56b908e9d9b7333ca483f6"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/kerf.git",
          "revisionId": "2b0cc5bda8cec604d294a70e25e7c4d33ac8ce58",
          "branch": "main"
        }
      ]
    }
  ]
}
