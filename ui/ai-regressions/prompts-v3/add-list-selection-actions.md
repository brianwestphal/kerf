Extend the existing release-task workspace so users can select task rows and act on the selection. Show a compact action area only while at least one task is selected, support select-all and clear-selection, announce changes, and preserve loading, empty, stale, and error states. Return complete contents only for files you changed; reuse the application’s existing data and action flow.
